// Shared build configuration. Local credentials are loaded when the ignored
// Secrets.xcconfig file exists, while clean checkouts still build with empty values.
FREESOUND_API_KEY =

#include? "Secrets.xcconfig"
